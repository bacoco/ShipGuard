interface:
  display_name: "ShipGuard Gauntlet (Deprecated)"
  short_description: "Legacy alias for ShipGuard Beat Reference"
  default_prompt: "Use $sg-gauntlet as the legacy alias for $sg-beat-reference."

policy:
  allow_implicit_invocation: false
