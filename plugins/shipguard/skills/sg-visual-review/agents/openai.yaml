interface:
  display_name: "ShipGuard Visual Review"
  short_description: "Review visual, code, logic, and process evidence"
  default_prompt: "Use $sg-visual-review to open the unified ShipGuard evidence dashboard."

policy:
  allow_implicit_invocation: false

dependencies:
  tools:
    - type: "shell"
      value: "node"
      description: "Build or serve the review dashboard."
    - type: "shell"
      value: "agent-browser"
      description: "Optionally open the dashboard."
