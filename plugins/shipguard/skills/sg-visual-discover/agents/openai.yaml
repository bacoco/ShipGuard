interface:
  display_name: "ShipGuard Visual Discover"
  short_description: "Discover UI routes and draft visual test manifests."
  default_prompt: "Discover ShipGuard visual scenarios for this app."

dependencies:
  tools:
    - type: "shell"
      value: "agent-browser"
      description: "Inspect routes and UI affordances in the browser."
    - type: "shell"
      value: "git"
      description: "Scope discovery to changed UI files when requested."
